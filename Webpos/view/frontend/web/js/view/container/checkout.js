define([
    'Magestore_Webpos/js/view/container/abstract'
], function (Container) {
    'use strict';

    return Container.extend({
        defaults: {
            container_id:"checkout"
        }
    });
});